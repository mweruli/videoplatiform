# Copy to .env for local dev.

# Base URL of the backend API (FastAPI). Matches docker-compose.yml's
# backend service by default when running the frontend outside Docker.
VITE_API_BASE_URL=http://localhost:8000
