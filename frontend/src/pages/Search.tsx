/**
 * Search & Discovery — Sprint 4 (Meilisearch integration, unified results
 * page, category/location/price filters). Stub route so navigation and the
 * page shell exist before the real build starts.
 */
export default function Search() {
  return (
    <div className="mx-auto w-full max-w-3xl px-5 py-16 text-center lg:px-8">
      <h1 className="font-display text-2xl font-bold tracking-tight">Search &amp; Discovery</h1>
      <p className="mt-2 text-sm text-muted-foreground">
        Coming in Sprint 4 — unified search across products, companies, videos, education content.
      </p>
    </div>
  )
}
