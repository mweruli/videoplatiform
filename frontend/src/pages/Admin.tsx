/**
 * Admin panel — Sprint 2+ (users, businesses, listings, moderation queue;
 * same React app, role-gated per the architecture in docs/DEVELOPMENT_PLAN.md).
 * Stub route.
 */
export default function Admin() {
  return (
    <div className="mx-auto w-full max-w-3xl px-5 py-16 text-center lg:px-8">
      <h1 className="font-display text-2xl font-bold tracking-tight">Admin</h1>
      <p className="mt-2 text-sm text-muted-foreground">
        Coming in Sprint 2+ — centralised admin control (users, businesses, moderation, ads).
      </p>
    </div>
  )
}
